/.cache
/.work
/_output
cover.out
/vendor
/.vendor-new

/package
# Legacy package directory is still ignored in case it is still sticking around locally
/stack-package

# ignore vscode files (debug config etc...)
/.vscode
