apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.0
  creationTimestamp: null
  name: trialcomponents.sagemaker.aws.crossplane.io
spec:
  group: sagemaker.aws.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - aws
    kind: TrialComponent
    listKind: TrialComponentList
    plural: trialcomponents
    singular: trialcomponent
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TrialComponent is the Schema for the TrialComponents API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: TrialComponentSpec defines the desired state of TrialComponent
            properties:
              deletionPolicy:
                description: DeletionPolicy specifies what will happen to the underlying external when this managed resource is deleted - either "Delete" or "Orphan" the external resource. The "Delete" policy is the default when no policy is specified.
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                description: TrialComponentParameters defines the desired state of TrialComponent
                properties:
                  displayName:
                    description: The name of the component as displayed. The name doesn't need to be unique. If DisplayName isn't specified, TrialComponentName is displayed.
                    type: string
                  endTime:
                    description: When the component ended.
                    format: date-time
                    type: string
                  inputArtifacts:
                    additionalProperties:
                      properties:
                        mediaType:
                          type: string
                        value:
                          type: string
                      type: object
                    description: The input artifacts for the component. Examples of input artifacts are datasets, algorithms, hyperparameters, source code, and instance types.
                    type: object
                  outputArtifacts:
                    additionalProperties:
                      properties:
                        mediaType:
                          type: string
                        value:
                          type: string
                      type: object
                    description: The output artifacts for the component. Examples of output artifacts are metrics, snapshots, logs, and images.
                    type: object
                  parameters:
                    additionalProperties:
                      properties:
                        numberValue:
                          type: number
                        stringValue:
                          type: string
                      type: object
                    description: The hyperparameters for the component.
                    type: object
                  region:
                    description: Region is which region the TrialComponent will be created.
                    type: string
                  startTime:
                    description: When the component started.
                    format: date-time
                    type: string
                  status:
                    description: "The status of the component. States include: \n    * InProgress \n    * Completed \n    * Failed"
                    properties:
                      message:
                        type: string
                      primaryStatus:
                        type: string
                    type: object
                  tags:
                    description: A list of tags to associate with the component. You can use Search API to search on the tags.
                    items:
                      properties:
                        key:
                          type: string
                        value:
                          type: string
                      type: object
                    type: array
                  trialComponentName:
                    description: The name of the component. The name must be unique in your AWS account and is not case-sensitive.
                    type: string
                required:
                - region
                - trialComponentName
                type: object
              providerConfigRef:
                description: ProviderConfigReference specifies how the provider that will be used to create, observe, update, and delete this managed resource should be configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - name
                type: object
              providerRef:
                description: 'ProviderReference specifies the provider that will be used to create, observe, update, and delete this managed resource. Deprecated: Please use ProviderConfigReference, i.e. `providerConfigRef`'
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: WriteConnectionSecretToReference specifies the namespace and name of a Secret to which any connection details for this managed resource should be written. Connection details frequently include the endpoint, username, and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: TrialComponentStatus defines the observed state of TrialComponent.
            properties:
              atProvider:
                description: TrialComponentObservation defines the observed state of TrialComponent
                properties:
                  trialComponentARN:
                    description: The Amazon Resource Name (ARN) of the trial component.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time this condition transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: A Message containing details about this condition's last transition from one status to another, if any.
                      type: string
                    reason:
                      description: A Reason for this condition's last transition from one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True, False, or Unknown?
                      type: string
                    type:
                      description: Type of this condition. At most one of each condition type may apply to a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
            required:
            - atProvider
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
