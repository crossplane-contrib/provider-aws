apiVersion: kms.aws.crossplane.io/v1alpha1
kind: Alias
metadata:
  name: dev-key
spec:
  forProvider:
    region: us-east-1
    aliasName: alias/dev-key
    targetKeyIDRef:
      name: dev-key
  providerConfigRef:
    name: example
