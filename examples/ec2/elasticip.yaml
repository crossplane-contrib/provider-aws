apiVersion: ec2.aws.crossplane.io/v1alpha1
kind: ElasticIP
metadata:
  name: "eip"
spec:
  forProvider:
    region: us-east-1
    domain: "vpc"
  reclaimPolicy: Delete
  providerConfigRef:
    name: example