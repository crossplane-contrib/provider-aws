---
apiVersion: ec2.aws.crossplane.io/v1alpha1
kind: RouteTable
metadata:
  name: routetable-without-routes
spec:
  forProvider:
    region: us-east-1
    vpcIdRef:
      name: sample-vpc
  providerConfigRef:
    name: example
