apiVersion: efs.aws.crossplane.io/v1alpha1
kind: FileSystem
metadata:
  name: example
spec:
  deletionPolicy: Orphan
  forProvider:
    region: us-east-1
    creationToken: unique-token
  providerConfigRef:
    name: default
