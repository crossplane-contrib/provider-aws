---
apiVersion: identity.aws.crossplane.io/v1alpha1
kind: IAMAccessKey
metadata:
  name: test-accesskey
spec:
  forProvider:
    userNameSelector:
      matchLabels:
        example: "true"
  providerConfigRef:
    name: example
  writeConnectionSecretToRef:
    name: access-key-secret
    namespace: default
