apiVersion: ecr.aws.crossplane.io/v1alpha1
kind: Repository
metadata:
  name: example
spec:
  forProvider:
    region: us-east-1
    imageScanningConfiguration:
      scanOnPush: true
    imageTagMutability: IMMUTABLE
  providerConfigRef:
    name: example
