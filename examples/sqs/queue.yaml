apiVersion: sqs.aws.crossplane.io/v1beta1
kind: Queue
metadata:
  name: sample-queue
spec:
  forProvider:
    region: us-east-1
    delaySeconds: 4
    redrivePolicy:
      deadLetterQueueArnRef:
        name: sample-queue2
  providerConfigRef:
    name: example
---
apiVersion: sqs.aws.crossplane.io/v1beta1
kind: Queue
metadata:
  name: sample-queue2
spec:
  forProvider:
    region: us-east-1
    delaySeconds: 4
  providerConfigRef:
    name: example