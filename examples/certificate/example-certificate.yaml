apiVersion: certificatemanager.crossplane.io/v1alpha1
kind: Certificate
metadata:
  name: infracloudcert
  annotations:
    crossplane.io/external-name: infracloudcert
spec:
  forProvider:
    domainName: infracloud.site
    validationMethod: DNS
  reclaimPolicy: Delete
  providerRef:
    name: example
