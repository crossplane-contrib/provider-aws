apiVersion: certificatemanager.aws.crossplane.io/v1alpha1
kind: Certificate
metadata:
  name: public-cert
  annotations:
    crossplane.io/external-name: public-cert
spec:
  forProvider:
    domainName: infracloud.site
    validationMethod: DNS
  reclaimPolicy: Delete
  providerRef:
    name: example
