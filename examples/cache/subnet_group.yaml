apiVersion: cache.aws.crossplane.io/v1alpha1
kind: CacheSubnetGroup
metadata:
  name: sample-subnet
  annotations:
    crossplane.io/external-name: some-subnet-name
spec:
  forProvider:
    description: desc for subnet
    subnetIds:
      - subnet-0bbdf4138f2e59b30
  reclaimPolicy: Delete
  providerRef:
    name: example
