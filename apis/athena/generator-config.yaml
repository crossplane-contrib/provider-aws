ignore:
  resource_names:
    - DataCatalog
    - NamedQuery
    - PreparedStatement
  field_paths:
    - CreateWorkGroupInput.Name
resources:
  WorkGroup:
    exceptions:
      errors:
        404:
          code: InvalidRequestException
