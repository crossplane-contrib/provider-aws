id: s3bucketclass
title: S3 Bucket Class
titlePlural: S3 Bucket Classes
category: Storage
overviewShort: "An S3BucketClass is a non-portable resource class. It defines the desired spec of resource claims that use it to dynamically provision a managed resource."
overview: |
 An S3BucketClass is a non-portable resource class. It defines the desired spec of resource claims that use it to dynamically provision a managed resource.
readme: |
 Cloud-specific resource classes are used to define a reusable configuration for a specific managed service. This class provides a object storage bucks, which is satisfied by [AWS S3](https://aws.amazon.com/s3/).

 Full documentation can be found in the Crossplane [API Reference docs](https://doc.crds.dev/github.com/crossplane/provider-aws/storage.aws.crossplane.io_s3bucketclasses.yaml).